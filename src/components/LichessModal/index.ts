import { connect } from 'react-redux';

// import { RootState } from 'types/state';

import LichessModal from './component';
import './style.scss';

const mapStateToProps = () => ({
});

export default connect(mapStateToProps, {})(LichessModal);
