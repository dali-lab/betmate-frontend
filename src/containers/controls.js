import React from 'react';

const Controls = (props) => {
  return (
    <div>
      <button type="button">+</button>
      <button type="button">-</button>
    </div>
  );
};

export default Controls;
